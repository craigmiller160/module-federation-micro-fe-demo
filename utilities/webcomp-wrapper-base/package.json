{
  "name": "@mfdemo/webcomp-wrapper-base",
  "version": "0.0.1-beta",
  "main": "index.js",
  "license": "MIT",
  "files": [
    "lib",
    ".yalc"
  ],
  "scripts": {
    "buildBabel": "rm -rf lib && babel src --out-dir lib",
    "build": "rm -rf lib && mkdir lib && cp src/* lib"
  },
  "devDependencies": {
    "@babel/cli": "^7.14.8",
    "@babel/core": "^7.15.0",
    "@babel/preset-env": "^7.15.0"
  }
}
